# cardiac_agent/requirements.txt
# Python 3.10 + PyTorch 2.2.2 + CUDA 12.1

# ── Transformers 生态 ─────────────────────────────────────────────────────────
transformers==4.40.2
tokenizers>=0.19,<0.20
accelerate==0.30.1
safetensors>=0.4.3
huggingface-hub>=0.23.0

# ── Embedding + 向量检索 ──────────────────────────────────────────────────────
sentence-transformers==2.7.0
# faiss-cpu 建议通过 conda 安装（更稳定），pip 备用：
faiss-cpu==1.7.4

# ── 量化推理 ──────────────────────────────────────────────────────────────────
bitsandbytes==0.43.1

# ── 基础依赖 ──────────────────────────────────────────────────────────────────
numpy>=1.24.0,<2.0
tqdm>=4.66.0
packaging>=23.0
regex>=2023.10.3
requests>=2.31.0
filelock>=3.13.0
pyyaml>=6.0.1
