# cardiac_agent/requirements.txt
# Python 3.10 + PyTorch 2.2.2 + CUDA 12.1
#
# 安装命令（一条）：
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121
#
# CPU 模式（没有 GPU）：
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu

# ── PyTorch ──────────────────────────────────────────────────────────────────
# pip 会优先去 --extra-index-url 找带 cu121 后缀的 whl，找不到再去 PyPI
# 所以这里写版本号即可，不需要硬编码 URL
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2

# ── Transformers 生态 ─────────────────────────────────────────────────────────
transformers==4.40.2
tokenizers>=0.19,<0.20
accelerate==0.30.1
safetensors>=0.4.3
huggingface-hub>=0.23.0

# ── Embedding + 向量检索 ──────────────────────────────────────────────────────
sentence-transformers==2.7.0
# faiss-cpu 建议通过 conda 安装（更稳定），pip 备用：
faiss-cpu==1.7.4

# ── 量化推理 ──────────────────────────────────────────────────────────────────
bitsandbytes==0.43.1

# ── 基础依赖 ──────────────────────────────────────────────────────────────────
numpy>=1.24.0,<2.0
tqdm>=4.66.0
packaging>=23.0
regex>=2023.10.3
requests>=2.31.0
filelock>=3.13.0
pyyaml>=6.0.1